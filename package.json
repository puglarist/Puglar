{
  "name": "puglar",
  "version": "0.1.0",
  "private": true,
  "description": "Puglar PWA starter with Prisma schema",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check web/app.js && node --check web/sw.js && node --check scripts/build.mjs"
  },
  "devDependencies": {
    "prisma": "^6.3.1"
  }
}
