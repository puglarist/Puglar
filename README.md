# Puglar

A concept GTA-style tactical HUD and inventory UX mockup inspired by the provided references.

## Run locally

Open `index.html` directly, or serve the folder:

```bash
python3 -m http.server 4173
```

Then visit `http://localhost:4173`.
